import { IComponentConfig, IInputs } from '../interface';
import Hook from './hook';
declare class ComponentExec {
    private config;
    protected hook: Hook;
    private projectConfig;
    constructor(config: IComponentConfig);
    private handleCredentials;
    init(): Promise<any>;
    private getNewActions;
    private executeCommand;
    private debugForJest;
    invokeMethod(instance: any, inputs: IInputs): Promise<any>;
}
export default ComponentExec;
