import { IActionHook, IInputs } from '../interface';
declare class Hook {
    private inputs;
    private preHooks;
    private afterHooks;
    private output;
    constructor(inputs: IInputs);
    init(hooks?: IActionHook[]): this;
    executePreHook(): Promise<any>;
    executeAfterHook({ output }: {
        output: any;
    }): Promise<void>;
    private commandExecute;
    private execComponent;
}
export default Hook;
