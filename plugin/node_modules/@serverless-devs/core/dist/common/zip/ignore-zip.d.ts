export interface IgnoreOptions {
    codeUri: string;
    ignoreFiles?: string[];
    include?: string[];
    outputFilePath?: string;
    outputFileName?: string;
}
export declare function ignoreZip(options: IgnoreOptions): Promise<unknown>;
