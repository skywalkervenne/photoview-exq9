import { DecompressOptions } from 'decompress';
export interface IOptions extends DecompressOptions {
    /**
     * If set to true, try extracting the file using decompress.
     */
    extract?: boolean;
    /**
     * Name of the saved file, default value is demo.zip
     */
    filename?: string;
}
declare const _default: (url: string, dest: string, options?: IOptions) => Promise<void>;
export default _default;
