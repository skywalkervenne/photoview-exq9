interface IConfig {
    type: 'pv' | 'action' | 'jsError' | 'networkError' | 'initTemplate' | 'installError';
    content?: string;
    traceId?: string;
}
declare function report(config: IConfig): Promise<void>;
export default report;
