import { StdioOption } from 'execa';
interface IOptions {
    cwd?: string;
    production?: boolean;
    stdio?: 'pipe' | 'ignore' | 'inherit' | readonly StdioOption[];
    showLoading?: boolean;
    graceInstall?: boolean;
}
declare function installDependency(options?: IOptions): Promise<void>;
export default installDependency;
